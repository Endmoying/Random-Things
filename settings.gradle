pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            name = "NeoForged"
            url = "https://maven.neoforged.net/releases"
        }
    }
}

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.5.0"
}

rootProject.name = "RandomThings"

include("randomcore")
include("randomthings-base")

include("randomthings")
